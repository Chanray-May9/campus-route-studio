# 安卓版使用说明

下载 `CampusRoute-Mobile.apk` 安装，支持 Android 8 及以上版本，独立版运行时不需要连接电脑。

手机开发者选项 → 模拟位置应用 → 校园路线。允许精确定位和通知，开启手机定位开关。地图点击添加点、拖动调整，也可输入学校坐标或读取手机近期位置。保存轨迹与导出 JSON 备份，设置速度、变速、摆动与指定圈数/无限循环，核对学校场地要求后开始。

回放启动后可切换到其他 App，也可从通知栏停止。部分手机的电池管理会结束后台服务，可在系统设置里允许“校园路线”后台运行。覆盖安装新版会保留本地轨迹，更新前仍建议导出 JSON 备份。

云端地址：https://campus-route-backend.gqr20080509.workers.dev
远程轨迹管理页为 `/admin`。管理员密码只应保存在维护者的私有环境中，不要写入仓库或 APK。

普通安卓仅模拟位置路径，不注入加速度；iPhone 仍需桌面版连接。学校规定只能在操场跑时，必须核对准确场地与实际路径并遵守规定。

源码：GPL-3.0-or-later；Leaflet：BSD-2-Clause。构建和部署步骤见 README.md 与 cloudflare/README.md。
